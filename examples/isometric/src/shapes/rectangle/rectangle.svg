<g
    highlighter-selector="base"
>
    <!-- Footprint — 2D representation, hit area, and the visible bottom
         plate in ISO. Uses the Iso variant so the bottom corners get cut
         when chamferBottomSize > 0 (matches the chamfered 3D side faces). -->
    <path @selector="base" @group-selector="common"
        :d="{{baseRectanglePathIso([size, cornerRadius, chamferBottomSize])}}"
        fill="#e0e0e0"
        stroke="#333"
        stroke-linejoin="round"
    />

    <!-- ISO: front face (bottom edge V2→V3) -->
    <path @selector="front" @group-selector="iso"
        fill="#e0e0e0"
        stroke="#333"
        stroke-linejoin="round"
        :d="{{rectangleFrontPath([size, isometricHeight, cornerRadius, chamferSize, chamferStart, chamferBottomSize, chamferBottomStart, twist, scaleTopX, scaleTopY, shedRoofDrop, shedRoofDirection, shapeRotation])}}"
    />

    <!-- ISO: side face (right edge V1→V2) -->
    <path @selector="side" @group-selector="iso"
        fill="#c6c6c6"
        stroke="#333"
        stroke-linejoin="round"
        :d="{{rectangleSidePath([size, isometricHeight, cornerRadius, chamferSize, chamferStart, chamferBottomSize, chamferBottomStart, twist, scaleTopX, scaleTopY, shedRoofDrop, shedRoofDirection, shapeRotation])}}"
    />

    <!-- ISO: corner strips — rendered before top face -->
    <path @selector="cornerV1" @group-selector="iso"
        fill="#c6c6c6"
        stroke="#333"
        stroke-linejoin="round"
        :d="{{rectangleCornerV1Path([size, isometricHeight, cornerRadius, chamferSize, chamferStart, chamferBottomSize, chamferBottomStart, twist, scaleTopX, scaleTopY, shedRoofDrop, shedRoofDirection, shapeRotation])}}"
    />
    <path @selector="cornerV2" @group-selector="iso"
        fill="#c6c6c6"
        stroke="#333"
        stroke-linejoin="round"
        :d="{{rectangleCornerV2Path([size, isometricHeight, cornerRadius, chamferSize, chamferStart, chamferBottomSize, chamferBottomStart, twist, scaleTopX, scaleTopY, shedRoofDrop, shedRoofDirection, shapeRotation])}}"
    />
    <path @selector="cornerV3" @group-selector="iso"
        fill="#e0e0e0"
        stroke="#333"
        stroke-linejoin="round"
        :d="{{rectangleCornerV3Path([size, isometricHeight, cornerRadius, chamferSize, chamferStart, chamferBottomSize, chamferBottomStart, twist, scaleTopX, scaleTopY, shedRoofDrop, shedRoofDirection, shapeRotation])}}"
    />

    <!-- ISO: top face — same footprint path shifted by (-iH, -iH) -->
    <path @selector="top" @group-selector="iso"
        fill="#a8a8a8"
        stroke="#333"
        stroke-linejoin="round"
        :d="{{topRectanglePath([size, isometricHeight, cornerRadius, chamferSize, chamferStart, chamferBottomSize, chamferBottomStart, twist, scaleTopX, scaleTopY, shedRoofDrop, shedRoofDirection, shapeRotation])}}"
    />

    <!-- ISO: icon image on the top face; position/size set via attr() -->
    <image @selector="topIcon" @group-selector="iso"
        href=""
        x="0" y="0" width="0" height="0"
        preserveAspectRatio="xMidYMid meet"
    />

    <!-- 2D: icon image centered in the shape bounding box; position/size set via attr() -->
    <image @selector="topIcon2D" @group-selector="2d"
        href=""
        x="0" y="0" width="0" height="0"
        preserveAspectRatio="xMidYMid meet"
    />

    <text @selector="label" @group-selector="common"
        text-anchor="left"
        text-vertical-anchor="middle"
        font-family="sans-serif"
        font-size="11"
        fill="#333"
        stroke="#fff"
        stroke-width="3"
        paint-order="stroke"
        y="calc(h + 10)"
        x="calc(w + 10)"
        text=""
    />
</g>
